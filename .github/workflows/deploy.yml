name: Deploy a GitHub Pages
on:
  push: { branches: [main] }
  workflow_dispatch:
permissions: { contents: read, pages: write, id-token: write }
concurrency: { group: pages, cancel-in-progress: true }

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - run: npm ci
      # Detecta solo dónde va a vivir la app:
      #  - con dominio propio (archivo public/CNAME) → raíz del dominio
      #  - sin dominio propio → subcarpeta con el nombre del repositorio
      - name: Construir
        run: |
          if [ -f public/CNAME ]; then
            echo "Dominio propio detectado: $(cat public/CNAME)"
            VITE_BASE="/" npm run build
          else
            echo "Sin dominio propio, usando /${GITHUB_REPOSITORY#*/}/"
            VITE_BASE="/${GITHUB_REPOSITORY#*/}/" npm run build
          fi
      - uses: actions/upload-pages-artifact@v3
        with: { path: dist }
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: { name: github-pages, url: "${{ steps.deployment.outputs.page_url }}" }
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
